import MainReaderInterface from "../../features/main-reader-interface";

export const metadata = {
  title: "SpeedReader Library",
  description: "Browse, upload, and manage your speed reading library.",
};

export default function MainReaderInterfacePage() {
  return <MainReaderInterface />;
}
