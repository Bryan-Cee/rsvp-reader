import SettingsConfiguration from "../../features/settings-configuration";

export const metadata = {
  title: "Settings Configuration - SpeedReader",
  description:
    "Customize your reading experience with comprehensive controls for speed, typography, behavior, and themes.",
};

export default function SettingsConfigurationPage() {
  return <SettingsConfiguration />;
}
